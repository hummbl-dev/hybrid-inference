name: CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Validate EDR schema hash pin
        run: |
          expected="2228890cb38c4d89a2a4ab46ba1b1430c2066f8c74548b8beb95e298f87ad45a"
          actual="$(sha256sum schemas/edr/EDR_v1.0.0.json | awk '{print $1}')"
          test "$actual" = "$expected"

      - name: Verify CAES spec hash pin
        run: sha256sum -c governance/CAES_CANONICAL.sha256

      - name: Validate replay report schema hash pin
        run: |
          expected="84362e7b44c4528048fb66625dc886c5548730ef4c5ff53273d26d89aeedd3b8"
          actual="$(sha256sum schemas/replay/REPLAY_REPORT_v1.0.0.json | awk '{print $1}')"
          test "$actual" = "$expected"

      - name: Replay invariant adversarial gates
        run: pytest -q tests/test_edr_replay.py

      - name: Replay reason-code contract lock gate
        run: pytest -q tests/test_edr_replay.py -k "reason_code_contract_lock or reason_codes_emitted_are_within_contract"

      - name: Authority invariant adversarial gates
        run: pytest -q tests/test_authority.py

      - name: Run tests
        run: pytest -q
